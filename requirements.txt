--extra-index-url https://download.pytorch.org/whl/cu126
# MMDetection의 mmcv는 torch와 CUDA에 맞춰 컴파일된 확장을 함께 싣습니다. 공식
# openmmlab 색인에는 torch 2.12 + cu126 wheel이 없어(cu126/torch2.12.0 -> 404) 아래
# 색인에서 받습니다. 이 색인이 내려가면 설치와 CI가 함께 멈춥니다.
--extra-index-url https://miropsota.github.io/torch_packages_builder

boto3==1.43.63
fastapi==0.141.1
httpx==0.28.1
# mmcv-lite를 함께 깔면 안 됩니다. 그쪽은 CUDA 연산자가 없어서 mmcv._ext를 찾지
# 못하고, DINO의 MultiScaleDeformableAttention과 Cascade R-CNN의 RoIAlign이 모두
# import 단계에서 멈춥니다.
mmcv==2.2.0+a8073c7pt2.12.0cu126
# mmdet 3.3.0은 mmcv 2.2.0 미만을 요구하지만 2024-01 이후 새 release가 없고, 위
# 조합이 유일하게 설치 가능한 wheel입니다. mmcv 2.2.0에는 2.1.0 대비 breaking
# change가 없어 각 pipeline이 그 상한만 좁게 통과시킵니다.
mmdet==3.3.0
mmengine==0.10.7
numpy==2.0.2
Pillow==11.3.0
pycocotools==2.0.11
pytest==8.3.5
torch==2.12.1+cu126
torchvision==0.27.1+cu126
uvicorn==0.52.1
