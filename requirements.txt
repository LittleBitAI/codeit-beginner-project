--extra-index-url https://download.pytorch.org/whl/cu126
# MMDetection의 mmcv는 torch와 CUDA에 맞춰 컴파일된 확장을 함께 싣습니다. 공식
# openmmlab 색인에는 torch 2.12 + cu126 wheel이 없어(cu126/torch2.12.0 -> 404) 아래
# 경로에서 받습니다. 이 경로가 내려가면 설치와 CI가 함께 멈춥니다.
#
# index가 아니라 find-links이고, 그것도 mmcv 하위 페이지만 가리킵니다.
# extra-index-url은 **모든 package**의 후보에 더해지는데 pip는 색인 사이에 우선순위를
# 두지 않고 전체 후보 중 가장 좋은 것을 고릅니다. 그래서 torch나 numpy까지 이 경로의
# 같은 이름 package로 바뀔 수 있습니다. find-links는 그 페이지에 있는 것만 후보로
# 삼고, 이 페이지에는 mmcv wheel밖에 없습니다.
#
# 남는 위험은 같은 파일이 나중에 다시 올라오는 경우입니다. pip의 hash 고정은 전부
# 아니면 전무라서 mmcv 하나에만 붙일 수 없으므로, local 꼬리표까지 정확히 못 박는
# 것으로 대신합니다.
--find-links https://miropsota.github.io/torch_packages_builder/mmcv/

boto3==1.43.63
fastapi==0.141.1
httpx==0.28.1
# mmcv-lite를 대신 깔면 안 됩니다. 그쪽은 CUDA 연산자가 없어서 mmcv._ext를 찾지
# 못하고, DINO의 MultiScaleDeformableAttention과 Cascade R-CNN의 RoIAlign이 모두
# import 단계에서 멈춥니다.
mmcv==2.2.0+a8073c7pt2.12.0cu126
# mmdet 3.3.0은 mmcv 2.2.0 미만을 요구하지만 2024-01 이후 새 release가 없고, 위
# 조합이 유일하게 설치 가능한 wheel입니다. mmcv 2.2.0에는 2.1.0 대비 breaking
# change가 없어 각 pipeline이 그 버전 하나에만 상한을 통과시킵니다.
mmdet==3.3.0
mmengine==0.10.7
numpy==2.0.2
Pillow==11.3.0
pycocotools==2.0.11
pytest==8.3.5
torch==2.12.1+cu126
torchvision==0.27.1+cu126
uvicorn==0.52.1
